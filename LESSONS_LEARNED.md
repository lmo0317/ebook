# 프로젝트 오류 기록 및 재발 방지 교훈 (Lessons Learned)

본 문서는 전자책 변환 및 AI 교정 시스템 개발 과정에서 발생한 실제 오류 사례와 그 원인, 재발 방지 해결책을 기록하는 지속적 학습 아카이브입니다.

---

## [사례 01] 구버전 캐시의 유령 히트로 인한 LLM 검수 전면 누락
- **발생 일시**: 2026-09-20
- **현상 (Symptom)**:
  - 348페이지의 전자책 변환 및 LLM 교정 작업이 1~2초 만에 즉시 완료됨.
  - 책 본문에는 여전히 `탤런 튜껑`, `특껑 테스트`, `제기워습니다` 등 명백한 OCR 오탈자가 그대로 남아 있음.
- **원인 분석 (Root Cause)**:
  1. 이전 테스트 때 생성된 캐시 파일(`llm_cache_*.json`)에 321페이지 분량의 데이터가 남아 있었음.
  2. 과거 캐시는 문단 번호(`'0'`, `'1'`)나 `list` 형태로 저장되어 있었으나, 최신 코드는 MD5 해시(`dict`)로 조회를 시도함.
  3. `if p_key in llm_cache:` 조건을 만족하자 "이미 캐시가 있다"고 오판했고, 포맷 불일치로 교체하지 못한 채 `return page_elements`로 조기 반환됨.
  4. 실제 로컬 12B 모델로 단 한 번의 요청도 가지 않은 채 스킵됨.
- **해결책 (Fix)**:
  - 캐시 데이터가 유효한 해시 딕셔너리(`any(len(k) == 16)`)가 아니거나 매칭된 문단이 0개인 경우, 조기 반환하지 않고 정상 LLM 추론을 타도록 방어 로직 구현.
  - GUI에 `[✓] 기존 LLM 캐시 초기화` 옵션 추가.
  - 손상되거나 오래된 캐시 파일 자동 탐지 및 백업 격리.

---

## [사례 02] 파이썬 정규식 `\b`와 한글 조사/어미 매칭 실패
- **발생 일시**: 2026-09-20
- **현상 (Symptom)**:
  - `(r"\b런되\b", "런팟")` 같은 정규식으로 치환을 시도했으나, 본문의 `런되올`, `런되(runpod)`가 전혀 치환되지 않음.
- **원인 분석 (Root Cause)**:
  - Python `re` 모듈의 `\b`(단어 경계)는 ASCII 문자 기준이므로 한글 단어 바로 뒤에 조사가 붙어 있으면 `\b`가 매칭되지 않음.
- **해결책 (Fix)**:
  - 한글 오탈자 정규식에서는 `\b`를 제거하거나, 룩어헤드/문맥 패턴을 사용할 것.

---

## [사례 03] Android Room Database 크래시 (Migration / Identity Hash)
- **발생 일시**: 2026-09-19
- **현상 (Symptom)**:
  - 파이썬으로 새로 빌드한 `reader.db`를 안드로이드 앱에 넣고 실행하면 `IllegalStateException: Room cannot verify the data integrity` 발생하며 앱 즉시 강제 종료.
- **원인 분석 (Root Cause)**:
  - Room 라이브러리는 빌드 시 생성된 `identity_hash`를 `room_master_table`에 기록하고 앱 시작 시 비교함.
- **해결책 (Fix)**:
  - SQLite DB 생성 시 `room_master_table`을 생성하고 해시값 `d5ff8686cb0182fe692967642ccb9c13`을 무조건 삽입하도록 고정.

---

## [사례 04] Windows PowerShell 콘솔 UTF-8 한글 깨짐
- **발생 일시**: 2026-09-20
- **현상 (Symptom)**:
  - 파이썬 출력 결과가 터미널에서 `ΰ` 같은 깨진 문자로 표시됨.
- **원인 분석 (Root Cause)**:
  - Windows 기본 콘솔 인코딩(CP949)과 파이썬 기본 stdout 인코딩 충돌.
- **해결책 (Fix)**:
  - 파이썬 스크립트 상단에 `sys.stdout.reconfigure(encoding="utf-8")` 명시 및 PowerShell 실행 시 `$env:PYTHONIOENCODING="utf-8"` 적용.

---

## [사례 05] 하드웨어 환경 타깃팅 혼선 (112 서버 vs 로컬 5080)
- **발생 일시**: 2026-09-20
- **현상 (Symptom)**:
  - 12B 대형 모델을 VRAM이 8GB에 불과한 112 원격 서버에서 실행하려다 OOM 발생 및 속도 저하.
- **원인 분석 (Root Cause)**:
  - 모델 크기(12B)에 필요한 VRAM(최소 10GB+)과 서버 사양 불일치. 로컬 PC는 RTX 5080(16GB VRAM)을 보유 중임.
- **해결책 (Fix)**:
  - 모든 LLM 교정 파이프라인의 기본 URL을 로컬 PC `http://127.0.0.1:8092`로 단일화.
